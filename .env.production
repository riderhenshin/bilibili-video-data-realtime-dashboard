# 静态部署环境 GitHub Pages部署
# Vite 会根据构建环境自动加载对应的 .env 文件：
# 开发环境（npm run dev）：加载 .env.development
# 生产环境（npm run build）：加载 .env.production（如果存在），否则使用默认值（空值或 .env 中的配置）
VITE_API_BASE_URL = '/bilibili-video-data-realtime-dashboard/'